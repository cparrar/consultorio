{% extends 'Template/base.html.twig' %}

{% block body %}

    <div class="col-md-12">
        <div class="panel panel-default no-bd">
            <div class="panel-header bg-dark">
                <h2 class="panel-title"><strong>Listado</strong> de Especialidades</h2>
            </div>
            <div class="panel-body bg-white">
                <div class="row">
                    <div class="col-md-12 col-sm-12 col-xs-12">
                        <form role="form" class="form-validation" novalidate="novalidate">
                            <div class="row">
                                <div class="col-sm-12">

                                    <table class="table">
                                        <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Nombre</th>
                                            <th>Estado</th>
                                            <th>Acciones</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% for especialidade in especialidades %}
                                            <tr>
                                                <td><a href="{{ path('especialidades_show', { 'id': especialidade.id }) }}">{{ especialidade.id }}</a></td>
                                                <td>{{ especialidade.nombre|title }}</td>
                                                <td>{% if especialidade.isActivo %}Activo{% else %}Inactivo{% endif %}</td>
                                                <td>
                                                    <a class="btn btn-sm btn-primary" href="{{ path('especialidades_show', { 'id': especialidade.id }) }}">Mostrar</a>
                                                    <a class="btn btn-sm btn-warning" href="{{ path('especialidades_edit', { 'id': especialidade.id }) }}">Editar</a>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>

                                </div>
                            </div>

                            {#<div class="text-center  m-t-20">#}
                            {#<button type="submit" class="btn btn-embossed btn-primary">Sign Up</button>#}
                            {#<button type="reset" class="cancel btn btn-embossed btn-default m-b-10 m-r-0">Cancel</button>#}
                            {#</div>#}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

